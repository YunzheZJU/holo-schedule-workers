{
  "name": "worker-youtube-watch",
  "version": "1.0.0",
  "description": "Extracting info from youtube's live.",
  "repository": "https://github.com/YunzheZJU/holo-schedule-workers",
  "main": "src/index.js",
  "author": "Yunzhe <yunzhe@zju.edu.cn>",
  "license": "MIT",
  "scripts": {
    "configure": "cd ../.. && yarn run configure worker-youtube-watch",
    "start": "wrangler preview --watch",
    "publish": "cd ../.. && yarn run publish worker-youtube-watch",
    "publish:all": "cd ../.. && yarn run publish:package worker-youtube-watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "cd ../.. && yarn run lint packages/worker-youtube-watch",
    "lint:fix": "yarn lint --fix"
  },
  "dependencies": {
    "@holo-schedule-workers/shared": "^0.1.0"
  }
}
